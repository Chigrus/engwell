const mysql = require('mysql');

let connection = null;

export async function init(){
    if(!connection){
        connection = await mysql.createPool({
            host: 'localhost',
            user: 'root',
            database: 'landing',
            password : ''
          });
    } 
    return {connection}
}